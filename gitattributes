# Auto detect text files and normalize them
* text=auto

# Declare files that should always have LF endings
*.sh text eol=lf
*.py text eol=lf

# Declare files that should always have CRLF endings (e.g. Windows batch files)
*.bat text eol=crlf
